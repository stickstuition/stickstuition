namespace MyRPG.Data
{
    public enum CharacterClassId { Knight, Mage, Ranger, Rogue }
    public enum CompanionId { None, Bramble, Lyra, Flint }
    public enum ItemCategory { Weapon, Armour, Helmet, Charm, DiceRelic, Consumable, Quest }
    public enum AbilityTarget { Self, SingleAlly, AllAllies, SingleEnemy, AllEnemies }
    public enum ArithmeticSubtopic { Addition, Subtraction, Multiplication, Division, OrderOfOperations, NegativeNumbers, FactorsAndMultiples }
    public enum QuestionFormat { Numerical, MultipleChoice }
}
